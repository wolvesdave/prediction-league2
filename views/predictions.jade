extends layout

block content
    if (!user)
      a(href="/login") Login
      br
      a(href="/register") Register
    if (user)
      a(href="/predictions") Predictions
      br
      a(href="/table") Table
      p You are currently logged in as #{user.email}
      a(href="/logout") Logout
      br

      form(role='form', action="/predictions", method="post")
        div(ng-controller="predictionController")
          .form-group
            Current Month
              input(type="text" name="month" value="#{month}" ng-model="month")
          .form-group
            Current Round
              input(type="number" name="round" value="#{round}" ng-model="round")
          .form-group
            div(class="col-xs-3") Home Team
            div(class="col-xs-1") Prediction
            div(class="col-xs-3") Away Team
            div(class="col-xs-1") Prediction
            div(class="col-xs-1") Actual
            div(class="col-xs-1") Score
            div(class="col-xs-1") Joker
          br
          .form-group
            div(ng-repeat="prediction in predictions")
              div(class="col-xs-3")
                input(type="text" class="form-control" name="homeTeam" value="{{ prediction.homeTeam }}" readonly required)
              div(class="col-xs-1")
                input(type="number" class="form-control" name="homePrediction" value="{{ prediction.homePrediction }}" required)
              div(class="col-xs-3")
                input(type="text" class="form-control" name="awayTeam" value="{{ prediction.awayTeam }}" readonly required)
              div(class="col-xs-1")
                input(type="number" class="form-control" name="awayPrediction" value="{{ prediction.awayPrediction }}" required)
              div(class="col-xs-1")
                input(type="number" class="form-control" name="homeScore" value="{{ prediction.homeScore }}" readonly required)
              div(class="col-xs-1")
                input(type="number" class="form-control" name="awayScore" value="{{ prediction.awayScore }}" readonly required)
              div(class="checkbox")
                label
                  input(type="hidden" name="joker" value="0")
                  input(type="checkbox" onclick="this.previousSibling.value=1-this.previousSibling.value")
          br
          button.btn.btn-default(type='submit') Submit
