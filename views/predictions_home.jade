extends layout

block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.email}
    a(href="/logout") Logout
    br
    a(href="/api/sysparms") Sysparms
